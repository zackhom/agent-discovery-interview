# agent-discovery-interview
This pipeline uses bm25 semantic search to find the top k agents cards that match a prompt. It then uses an interview agent to form questions and evaluate the responses of each k candidate. Candidate agents are accessed through the url endpoint in their agent cards.
